name: cbor unittest
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: python3 test_encode.py
      - run: echo "🍏 This job's status is ${{ job.status }}."

